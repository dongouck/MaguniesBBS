create table linksPage (
	category 	int,
	topic		varchar(200),
	url			varchar(300),
	likecount			int,
	dislikecount		int,
	date			date,
	writeer		varchar(100),
	primary key (topic)
	);


select now();
